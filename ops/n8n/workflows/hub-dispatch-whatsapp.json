{
  "name": "Hub Dispatch WhatsApp",
  "nodes": [
    {
      "parameters": {
        "path": "hub/dispatch/whatsapp",
        "responseMode": "responseNode",
        "httpMethod": "POST"
      },
      "id": "Webhook_WhatsApp",
      "name": "Webhook WhatsApp",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [220, 280]
    },
    {
      "parameters": {
        "jsCode": "const j = items[0].json || {}; const h = j.headers || {}; const signature = h['x-dispatch-signature'] || ''; const timestamp = h['x-dispatch-timestamp'] || ''; if (!signature || !timestamp) { return [{ json: { ok: false, error: 'MISSING_SIGNATURE_HEADERS', statusCode: 401 } }]; } if (!/^[a-f0-9]{64}$/i.test(signature)) { return [{ json: { ok: false, error: 'INVALID_SIGNATURE_FORMAT', statusCode: 401 } }]; } const tsMs = Date.parse(timestamp); if (Number.isNaN(tsMs)) { return [{ json: { ok: false, error: 'INVALID_TIMESTAMP', statusCode: 401 } }]; } const maxSkewMs = 5 * 60 * 1000; if (Math.abs(Date.now() - tsMs) > maxSkewMs) { return [{ json: { ok: false, error: 'STALE_TIMESTAMP', statusCode: 401 } }]; } return [{ json: { ok: true, provider: 'n8n-whatsapp', externalEventId: `wa_${Date.now()}`, receivedAt: new Date().toISOString(), statusCode: 200 } }];"
      },
      "id": "Code_Guard",
      "name": "Build Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [520, 280]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{$json}}",
        "options": { "responseCode": "={{$json.statusCode || 200}}" }
      },
      "id": "Respond_Final",
      "name": "Respond Final",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [860, 280]
    }
  ],
  "connections": {
    "Webhook WhatsApp": {
      "main": [[{ "node": "Build Response", "type": "main", "index": 0 }]]
    },
    "Build Response": {
      "main": [[{ "node": "Respond Final", "type": "main", "index": 0 }]]
    }
  },
  "settings": { "executionOrder": "v1" },
  "pinData": {},
  "active": false
}

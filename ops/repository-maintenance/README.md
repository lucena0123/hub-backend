# Repository Maintenance Scripts

These scripts are operational utilities for one-off repository and folder maintenance.

Run them only after reading the script header and verifying the expected working directory.
Several scripts can move folders, update GitHub repositories, or change local remotes.
